## catonline

# interact via command line thru stdin
`$ ./bin/do`

# init server
`$ ./bin/init`

# query database
`$ ./bin/db`

# dump database (to /data/dump/)
`$ ./bin/dump`

# pull most recent state-graph data
`$ ./bin/pull`
